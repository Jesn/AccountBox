# ============================================
# AccountBox 环境变量配置模板
# ============================================
# 复制此文件为 .env 并根据需要修改配置
# 注意：.env 文件不会被提交到 Git 仓库
# ============================================

# ============================================
# 数据库配置
# ============================================

# 数据库类型: sqlite | postgresql | mysql
DB_PROVIDER=sqlite

# SQLite 数据库路径（仅当 DB_PROVIDER=sqlite 时使用）
# DATABASE_PATH=/app/data/accountbox.db

# PostgreSQL 连接字符串（仅当 DB_PROVIDER=postgresql 时使用）
# CONNECTION_STRING=Host=localhost;Port=5432;Database=accountbox;Username=accountbox;Password=your-secure-password

# MySQL 连接字符串（仅当 DB_PROVIDER=mysql 时使用）
# CONNECTION_STRING=Server=localhost;Port=3306;Database=accountbox;User=accountbox;Password=your-secure-password

# ============================================
# 安全配置（可选 - 如果不设置将自动生成）
# ============================================

# JWT 密钥（推荐：至少 64 字符的随机字符串）
# 如果不设置，首次启动时会自动生成并保存到 data/.secrets/jwt.key
# JWT_SECRET_KEY=your-super-secret-jwt-key-at-least-64-characters-long

# 主密码（用于登录系统）
# 如果不设置，首次启动时会自动生成随机密码并显示在日志中
# MASTER_PASSWORD=your-secure-master-password

# 密钥存储目录（默认：./data）
# DATA_PATH=/app/data

# ============================================
# JWT 配置（可选）
# ============================================

# JWT 签发者
# JWT_ISSUER=AccountBox

# JWT 受众
# JWT_AUDIENCE=AccountBox-Web

# JWT 过期时间（分钟）
# JWT_EXPIRATION=1440

# ============================================
# CORS 配置（可选）
# ============================================

# 允许的前端地址（多个地址用逗号分隔）
# CORS_ORIGIN=http://localhost:5173,http://localhost:5093

# ============================================
# ASP.NET Core 配置
# ============================================

# 运行环境: Development | Production
ASPNETCORE_ENVIRONMENT=Production

# 监听地址
ASPNETCORE_URLS=http://+:5093

# ============================================
# Docker 部署配置（仅用于 docker-compose）
# ============================================

# 后端端口映射
# BACKEND_PORT=5093

# PostgreSQL 配置
# POSTGRES_DB=accountbox
# POSTGRES_USER=accountbox
# POSTGRES_PASSWORD=your-secure-password
# POSTGRES_PORT=5432

# MySQL 配置
# MYSQL_DATABASE=accountbox
# MYSQL_USER=accountbox
# MYSQL_PASSWORD=your-secure-password
# MYSQL_ROOT_PASSWORD=your-root-password
# MYSQL_PORT=3306
